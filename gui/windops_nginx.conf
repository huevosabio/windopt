server {
	root /mnt/windows/development/windops/dist;

	listen 80;
	server_name localhost;
	charset utf-8;

	client_max_body_size 75M;

	location / { try_files $uri @uwsgi; }
	location @uwsgi {
		uwsgi_pass unix:///tmp/windops_uwsgi.sock;
		include uwsgi_params;
	}
}